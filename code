#include<bits/stdc++.h>
using namespace std;

struct heap 
{ 
   int x;
   string s; 
}; 
  
void heapify(struct heap a[],int i,int n)
{
    int largest = i; 
    int l = 2*i + 1;  
    int r = 2*i + 2;
    if (l < n && a[l].x > a[largest].x) 
        largest = l;
    if (r < n && a[r].x > a[largest].x) 
        largest = r; 
        
    if (largest != i) 
    { 
        struct heap temp=a[i];
        a[i]=a[largest];
        a[largest]=temp;
        heapify(a,largest,n); 
    }     
}
  
void make_heap(struct heap a[],int n)
{
    int m=n/2;
    for(int i=m;i>=0;i--)
        heapify(a,i,n);
}

void pop(struct heap a[],int n)
{
    a[0].x=-1;
    heapify(a,0,n); 
}
  
int main() 
{
   struct heap a[5]; 
   for(int i=0;i<5;i++)
   {
       int t;
       string s;
       cin>>t>>s;
       a[i].x=t;
       a[i].s=s;
   }
   make_heap(a,5);
   cout<<a[0].x<<" "<<a[0].s<<endl;
   pop(a,5);
   cout<<a[0].x<<" "<<a[0].s<<endl;
   return 0; 
} 



//5 aman
//8 arpit
//2 nishant
//6 aditya
//3 anita
